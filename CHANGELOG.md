# 变更日志

本项目版本号对应 `fnos-app/manifest` 里的 `version`。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [1.0.8] — 2026-09-19

### 修复

- **默认触控模式从 Minitouch 改为 MaaTouch。**
  ChromeOS 版 redroid 镜像（`erstt/redroid:13.0.0_ndk_ChromeOS`）里没有虚拟触摸设备节点，
  Minitouch 初始化必失败（`Unable to find a suitable touch device`），装完默认配置直接连不上。
  MaaTouch 不依赖该节点，适配面更广。旧的标签（`MaaTouch（实验功能）` 等）保留映射，老配置不受影响。

- **「截图测试」在未连接时自动用当前配置发起一次连接。**
  之前 connect 失败后 `_asst` 不可用，用户切换触控模式后点「截图测试」恒报"未连接"，
  形成"换了模式也连不上"的假象。现在未连接时会先连一次（含触控模式初始化），
  连接失败时把原因带出来。

- **「截图测试」在配置不止一份时拿不到配置。**
  `_resolve_status_profile` 判断不了该用哪份就返回 `None`，界面恒定回
  「没有可用的任务档案」。改为前后端传递并使用当前编辑的档案名；
  没带名字、配置又不止一份时，也挑一份去连。

- **启动时容器挂的是旧镜像也不会重建。**
  之前只看"容器在不在"，在跑就直接复用 —— 升级装进来的新镜像永远不生效，跑的仍是旧代码。
  现在会比对容器镜像 ID 与包内期望值，不一致就重建容器。

- **镜像包解压判定过严。**
  `gunzip` 对「压缩流完好、尾部多带几个字节」的包返回 2（trailing garbage），
  解出来的 tar 其实是完整的，却被判成"解压失败"导致启动失败。
  判定改为校验解压产物本身（能否列出 `manifest.json`）。

### 备注

- fnOS 的 `appcenter-cli install-fpk` 对**已安装的同名应用不执行覆盖升级**
  （命令返回成功、但 appcenter 日志里没有任何 install 事件，应用目录文件不变）。
  手动更新需**先卸载再安装**；卸载脚本只删容器，`maa-data` 保留。

---

## [1.0.7] — 2026-09-19

### 变更
- **应用标识整体改名 `maa-web` → `maa-fnos`**：appname、容器名、
  镜像名（原 `maa-web-control:local` → `maa-fnos:local`）、数据目录
  （`/vol1/@appdata/maa-fnos/`）、fpk 文件名（`maa-fnos-v<版本>.fpk`）统一更换。
  原因：`maa-web` 系列名称取自上游前端项目，但容器里已是完整应用（引擎+前端+adb），名不副实。
- 旧容器名 `maa-web` 与 `maa-web-test` 均列入历史清理名单，启动时自动删除；
  redroid 状态代理白名单兼容新旧全部容器名。
- ⚠️ 因 appname 变更，已装旧版（≤1.0.6）的用户**不会收到升级提示**，需手动安装新包；
  旧配置在 `/vol1/@appdata/maa-web/maa-data/`，需手动迁入新目录
  （`/vol1/@appdata/maa-fnos/maa-data/`）。

---

## [1.0.6] — 2026-09-19

### 修复

- **redroid 状态代理的升级改动不生效，旧进程一直在跑**
  应用升级时会更新代理脚本，但启动探活用的是 `curl -s`（**没带 `-f`**）：探针名
  `__probe__` 不在白名单里，代理回 **403**，而 403 在 `curl -s` 眼里照样算"成功"。
  于是每次都判定"代理已在运行"、直接跳过启动，**新脚本永远没机会被加载**。

  后果是 1.0.5 里那条「redroid 状态查询对不上真实容器名」的修复事实上没生效 ——
  实测 18001 上的进程启动于 09-18 12:42，而脚本在 17:49 就被更新过，进程比脚本
  早了 5 个多小时，内存里跑的始终是旧代码。

  现在代理自带版本号（新增 `GET /version`），启动探活改为**比对版本**：版本一致
  才算就绪；不一致（或压根问不到版本，说明是本项目旧版代理）就先把旧进程停掉再起
  新的。停进程只按本脚本的完整路径精确匹配，不碰宿主机上其他 python 进程。

- **容器名改为按端口反查，不再靠猜**
  上一版的做法是"默认名查不到时，取本机第一个名字含 redroid 的容器"。宿主机上只
  要多一个名字带 redroid 的容器（旧的测试实例、别的镜像），就会认错。

  现在应用启动时把 redroid 的 ADB 端口（默认 5555）注入给代理，代理用
  `docker ps --filter publish=<端口>` 反查出真实容器名 —— 应用既然连得上这个端口，
  在监听它的容器就必然只有一个，比猜名字可靠得多。查找顺序：
  显式指定的 `MAA_REDROID_CONTAINER` → 按端口反查 → 名字含 redroid 的第一个容器。

  「自动检测连接」探测的就是同一个端口，两个按钮从此基于同一份事实，不会再见
  "一个说设备连得上、一个说容器不存在"的矛盾。

### 变更
- `manifest` 版本号 1.0.5 → 1.0.6

### 说明
- 本轮只改脚本（`cmd/main`、`redroid_status_proxy.py`）与元数据，**未改动应用镜像内容**，
  包内离线镜像沿用 1.0.4 的构建产物。
- 升级后容器会被重建；业务数据在 `/vol1/@appdata/maa-web/`，不受影响。

---

## [1.0.5] — 2026-09-18

### 修复

- **停应用时，进行中的任务链被硬切断**
  原先停应用直接 `docker stop`，正在跑的任务链会被硬切断且没有任何收尾，前端持有的
  run_id 随即失效 —— 表现为「运行着运行着它没了／网页能开但任务状态全是错的」。
  现在停机前先请应用停掉进行中的任务链，等它进终态再停容器：

  - 终态（`Idle` / `Stopped` / `Completed` / `Failed` / 空）直接跳过，不发多余请求
  - 已在 `Stopping` 只等待、不重复下发
  - 未知状态按「还在干活」处理，宁等勿断
  - 等待期间接口掉线则提前退出，交给 `docker stop` 兜底
  - 等满宽限期仍未落地则记一条 WARN 后继续停机，不卡死停机流程

- **redroid 状态查询对不上真实容器名**
  前端「检查 redroid 容器」不带容器名，代理固定用默认名 `redroid` 去查，而实际容器名
  往往不是它，于是永远查不到。现在可用环境变量 `MAA_REDROID_CONTAINER` 显式指定；
  未指定时若默认名查不到，自动落到本机第一个名字含 `redroid` 的容器上。

- **`adb` 被遗弃的僵尸进程长期堆积**
  容器内没有 init，PID 1 不回收子进程，`adb` 反复拉起后会留下 `<defunct>` 僵尸。
  启动参数补上 `--init`。

- **容器崩了就永久停摆，而应用中心仍显示「运行中」**
  原先重启策略是 `--restart no`，容器一旦因 OOM 或异常退出就再也不会回来；而应用中心的
  状态判定只看 `docker ps` 与 PID 文件，于是网页上一直显示「正常运行」，
  用户看到的是「应用活着但什么都干不了」。
  现在重启策略改为 `--restart unless-stopped`（在应用中心显式停用不会被自动拉起），
  并给 `status` 加上真实 HTTP 探活：**容器在 ≠ 服务在**，容器跑着但接口不通即判为异常。
  探活失败的 WARN 日志做 60 秒节流，避免把日志刷爆；容器启动后的宽限期内不探活，
  避免服务还没 listen 就误报。

### 变更

- `manifest` 版本号 1.0.4 → 1.0.5

### 说明

- 本轮只改脚本与容器启动参数，**没有改动应用镜像内容**，包内离线镜像沿用 1.0.4 的构建产物。
- 升级后容器会被重建；业务数据在 `/vol1/@appdata/maa-web/`，不受影响。

---

## [1.0.4] — 2026-09-18

### 修复
- **卸载重装后镜像指纹丢失，新镜像不会被导入**
  镜像指纹文件 `maa-image.id` 放在 `${TRIM_APPDEST}`，而 `app/` 的内容正好也摊平在那里，
  卸载会把整个目录清空，指纹文件跟着消失。重装时安装钩子只判断"同名镜像在不在"、
  在就直接跳过导入，于是指纹一直补不回来；而启动脚本在没有指纹文件时会退化成
  "存在即就绪"。结果就是：**包里换了镜像，装上去跑的仍是旧的，且毫无报错。**

  现在期望指纹改为**从包内镜像包本身推导**（`docker save` 产物的 `manifest.json` 里
  `Config` 指向的摘要即镜像 ID），安装钩子与启动脚本都用它做比对：
  - 安装钩子：每次安装都重新推导并落盘，实际 ID 不一致就先摘旧 tag 再导入，导入后复核
  - 启动脚本：指纹文件缺失时现场重新推导（自愈），不再有"没有指纹就当就绪"的退化分支

### 变更
- `manifest` 版本号 1.0.3 → 1.0.4

---

## [1.0.3] — 2026-09-18

### 变更
- **容器名改为 `maa-web`**（原 `maa-web-test`）
  升级时 `cmd/main` 会自动清理旧名容器：两个名字绑的是同一个宿主机端口 `18000`，
  旧容器留着会占端口、导致新容器起不来。卸载钩子 `uninstall_init` 同样会清掉两个名字。
- redroid 状态代理白名单放宽为 `^maa-web(-test)?$`，兼容前端里存过的旧容器名
- `manifest` 版本号 1.0.2 → 1.0.3

---

## [1.0.2] — 2026-09-18

### 修复
- **升级时旧镜像阻挡新镜像**（重要）
  `cmd/main` 的 `ensure_image()` 原先只判断「同名镜像是否存在」。
  由于 `docker load` 不会自动顶掉同名同 tag 的旧镜像，升级后旧镜像仍在，
  导致**新镜像永远不会被导入**，容器一直跑老版本。
  改为**镜像指纹比对**：把已导入镜像的 `docker image inspect --format '{{.Id}}'`
  写入 `${TRIM_APPDEST}/maa-image.id`，启动时比对；不一致就先 `docker rmi -f` 再重新导入。

### 变更
- `manifest` 版本号 1.0.1 → 1.0.2

---

## [1.0.1] — 2026-09-18

### 新增
- **redroid 状态查询代理**，修复「检查 redroid 容器」报「未找到 docker 命令」的问题：
  - 宿主侧：`redroid_status_proxy.py` 监听 `172.17.0.1:18001`，只读、白名单、仅支持 `/inspect?name=<容器>`
  - 容器侧：`/usr/local/bin/docker` 假脚本，只接受一条 `docker inspect --format …` 命令
  - **不挂载 `docker.sock`**，遵循最小权限原则
- 桌面图标补全至 12 个尺寸（16/24/32/48/64/72/96/128/144/256/512 + `icon_{0}.png`）

### 变更
- `manifest` 版本号 1.0.0 → 1.0.1

---

## [1.0.0] — 2026-09-17

### 新增
- 首个版本。把 MAA 官方 Linux 核心 + Web 前端打包成飞牛 fpk 应用：
  - 内置离线镜像（gzip 压缩的 `docker save` 产物），安装时自动导入，**无需联网**
  - `cmd/main` 完整生命周期：`start` / `stop` / `status`
  - 数据持久化到 `${TRIM_PKGVAR}/maa-data`，卸载重装不丢
